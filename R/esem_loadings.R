esem_loadings<-function(data, loadings){
  results<-1
  #make target rotation matrix for use with esem_efa - check source
  #Target matrix with NAs and zeros [nItems x nLatentVariables].


  #esemComp::make_target(nitems = 9, mainloadings = main_loadings_list)
  return (results)
  #need to read what it means
}
